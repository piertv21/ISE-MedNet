{
  "name": "mednet-tooling",
  "version": "1.0.0",
  "private": true,
  "description": "Git hooks tooling for the MedNet MAS project (husky + commitlint).",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "husky": "^9.1.7"
  }
}
